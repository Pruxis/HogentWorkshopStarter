import React from 'react';

type Props = React.InputHTMLAttributes<HTMLInputElement>;

/**
 * Opdracht 2: Maak een Input component.
 * Tip: maak gebruik van je editor en ctrl + spatie om geholpen te worden.
 * - Voor een input component gebruiken we de input html tag.
 * - Zorg ervoor dat je input een placeholder heeft.
 * - Zorg dat je een value MOET ingeven, m.a.w dat je waarde 'required' is.
 * - Het component moet de 'className' "register-input" hebben.
 */
const RegisterInput: React.FC<Props> = props => null;

export default RegisterInput;
